org.gradle.jvmargs=-Xmx1536m -Dfile.encoding=UTF-8
android.useAndroidX=false
android.nonTransitiveRClass=true
# NOTE: 'android.bundle.enableUncompressedNativeLibs' (removed in AGP 8.1) is
# intentionally not set. Native libs are forced to extract on install via
# jniLibs.useLegacyPackaging=true in app/build.gradle (the engine dlopens its
# modules from the filesystem).
